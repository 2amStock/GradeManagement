<Window x:Class="GradeManagement.GradeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradeManagement"
        mc:Ignorable="d"
        Title="GradeWindow" Height="450" Width="800" WindowStyle="SingleBorderWindow" WindowState="Maximized" Loaded="Window_Loaded">
    <Grid>
        <TabControl  x:Name="tabControl" SelectionChanged="tabControl_SelectionChanged" Margin="10">

            <TabItem Header="Quản lý điểm">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="559*"></ColumnDefinition>
                        <ColumnDefinition Width="241*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <ComboBox x:Name="cbCourses" SelectionChanged="cbCourse_SelectionChanged">

                        </ComboBox>
                        <DataGrid x:Name="dgStudentGrades" AutoGenerateColumns="False" SelectionMode="Single" IsReadOnly="True" SelectionChanged="dgStudentGrades_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Width="100" Header="FullName" Binding="{Binding FullName }" ></DataGridTextColumn>
                                <DataGridTextColumn Width="100" Header="RollNumber" Binding="{Binding RollNumber}"></DataGridTextColumn>

                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                    <StackPanel Grid.Column="1"  >


                        <TextBlock Text="Grade Details" FontSize="20" Width="240" Height="31" TextAlignment="Center" Background="#FF82C3FE"/>
                        <StackPanel x:Name="spGradeItemDetails" Margin="10" >
                            <!-- Các Label + TextBox sẽ được thêm động ở đây -->
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                            <Button x:Name="btnSave" Content="Save" Width="49" Height="20"  Background="#FFF11010" Margin="0,0,10,10" Click="btnSave_Click"/>
                            <Button x:Name="btnExport" Content="Export" Width="49" Height="20" Margin="0,0,10,10" Click="btnExport_Click"/>
                            <Button x:Name="btnImport" Content="Import" Width="49" Height="20" Margin="0,0,10,10" Click="btnImport_Click"/>
                            <Button x:Name="btnRefesh" Content="Refesh" Width="49" Height="20"  Background="#FF14F1F1" Margin="0,0,10,10" Click="btnRefesh_Click"/>
                            

                        </StackPanel>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- TAB 2: Thêm GradeItem -->
            <TabItem Header="Quản lý điểm thành phần">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Vertical" Margin="0 0 0 10">
                        <TextBlock Text="Môn học (Subject):"/>
                        <ComboBox x:Name="cbSubjects"  Width="300" SelectionChanged="cbSubjects_SelectionChanged"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0 0 0 10">
                        <TextBlock Text="Loại điểm (GradeCategory):"/>
                        <ComboBox x:Name="cbCategorys" Width="300"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Row="2" Margin="0 0 0 10">
                        <TextBlock Text="Tên GradeItem:"/>
                        <TextBox x:Name="txtGradeItemName" Width="300"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical" Grid.Row="3" Margin="0 0 0 10">
                        <TextBlock Text="Trọng số (%):"/>
                        <TextBox x:Name="txtWeight" Width="300"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Row="4" VerticalAlignment="Top">
                        <Button Content="Thêm" x:Name="btnAddGradeItem" Click="btnAddGradeItem_Click" Width="120" Margin="0 0 10 0"/>
                        <Button Content="Sửa" x:Name="btnEditGradeItem" Click="btnEditGradeItem_Click" Width="120" Margin="0 0 10 0"/>
                        <Button Content="Xóa" x:Name="btnDeleteGradeItem" Click="btnDeleteGradeItem_Click" Width="120" Margin="0 0 10 0"/>
                        <Button Content="Làm mới" x:Name="btnRefeshGradeItem" Click="btnRefeshGradeItem_Click"  Width="120" Margin="0 0 10 0"/>
                    </StackPanel>
                    <StackPanel Grid.Row="4" Margin="0 13 0 0">
                        <DataGrid SelectionMode="Single" IsReadOnly="True" x:Name="dgGradeItems" AutoGenerateColumns="False" Margin="0 10 0 0" SelectionChanged="dgGradeItems_SelectionChanged">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Môn học" Binding="{Binding SubjectId}" Width="*"/>
                                <DataGridTextColumn Header="Loại điểm" Binding="{Binding GradeCategory.GradeCategoryName}" Width="*"/>
                                <DataGridTextColumn Header="Tên GradeItem" Binding="{Binding GradeItemName}" Width="*"/>
                                <DataGridTextColumn Header="Trọng số (%)" Binding="{Binding Weight}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid> 
                    </StackPanel>
                </Grid>
            </TabItem>
            <!-- Tab3 -->
            <TabItem Header="Quản lý khóa học">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <Label Content="Class code"></Label>
                        <TextBox x:Name="txtClassCode" Width="200" Margin="10"/>
                        <Label Content="Môn học"></Label>
                        <ComboBox x:Name="cbSubjectsForCourseManage" Width="200" Margin="10"/>
                        <Label Content="Giảng viên"></Label>
                        <ComboBox x:Name="cbLecturersForCourseManage" Width="200" Margin="10"/>
                        <Label Content="Ngày bắt đầu"></Label>
                        <DatePicker x:Name="dpStartDate" Width="200" Margin="10"/>
                        <Label Content="Ngày kết thúc"></Label>
                        <DatePicker x:Name="dpEndDate" Width="200" Margin="10"/>
                    </StackPanel >
                    <StackPanel Grid.ColumnSpan="2" Grid.Row="1" Orientation="Horizontal" Margin="10">
                        <Button Content="Thêm khóa học" Width="120" Margin="5"/>
                        <Button Content="Sửa khóa học" Width="120" Margin="5" />
                        <Button Content="Xóa khóa học" Width="120" Margin="5" />
                    </StackPanel>

                    <DataGrid Grid.ColumnSpan="2" x:Name="dgCourses" Grid.Row="2" Margin="10" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="CourseID" Binding="{Binding CourseId}" Width="Auto"/>
                            <DataGridTextColumn Header="ClassCode" Binding="{Binding ClassCode}" Width="*"/>
                            <DataGridTextColumn Header="SubjectID" Binding="{Binding SubjectId}" Width="*"/>
                            <DataGridTextColumn Header="StartDate" Binding="{Binding StartDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                            <DataGridTextColumn Header="EndDate" Binding="{Binding EndDate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                            <DataGridTextColumn Header="Lecturer Name" Binding="{Binding Lecturer.FullName}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StatusBar Grid.Row="3" Grid.ColumnSpan="2">
                        <StatusBarItem Content="Số lượng khóa học: " />
                        <StatusBarItem x:Name="courseCount" Content="0"/>
                    </StatusBar>
                </Grid>
            </TabItem>

            <!-- Tab4 quản lý môn học theo kì -->
            <TabItem Header="Quản lý môn học theo">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="10">
                        <Button Content="Thêm môn học" Width="120" Margin="5" />
                        <Button Content="Sửa môn học" Width="120" Margin="5"/>
                        <Button Content="Xóa môn học" Width="120" Margin="5" />
                    </StackPanel>

                    <DataGrid x:Name="dgSubjects" Grid.Row="1" Margin="10" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SubjectId" Binding="{Binding SubjectId}" Width="*"/>
                            <DataGridTextColumn Header="SubjectName" Binding="{Binding SubjectName}" Width="2*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StatusBar Grid.Row="2">
                        <StatusBarItem Content="Số lượng môn học: " />
                        <StatusBarItem x:Name="subjectCount" Content="0"/>
                    </StatusBar>
                </Grid>
            </TabItem>

            <!-- Tab5 Quản lí giáo viên -->
            <TabItem Header="Quản lí giáo viên">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <Label Content="Thông tin giáo viên" Foreground="#FFF10F0F" FontWeight="Bold"></Label>
                        <Label Content="Tên giáo viên"></Label>
                        <TextBox x:Name="txtLecturerName"></TextBox>
                        <Label Content="Email"></Label>
                        <TextBox x:Name="txtLecturerEmail"></TextBox>
                        <Label Content="Số điện thoại"></Label>
                        <TextBox x:Name="txtLecturerPhone"></TextBox>
                        <Label Content="Ngày sinh"></Label>
                        <DatePicker x:Name="dpLecturerBirthDate" Margin="0,0,0,10"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Horizontal">
                        <Label Content="Tìm kiếm giáo viên theo tên:" Margin="10"/>
                        <TextBox x:Name="txtSearchLecturer" Width="200" Margin="10" />
                        <Button Content="Tìm kiếm" Width="80" Margin="10"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
                        <Button Content="Thêm giáo viên" Width="120" Margin="5" Click="ButtonAddLecturer_Click" />
                        <Button Content="Sửa giáo viên" Width="120" Margin="5" />
                        <Button Content="Xóa giáo viên" Width="120" Margin="5" />
                    </StackPanel>

                    <DataGrid x:Name="dgLecturers" Grid.Row="2" Grid.ColumnSpan="2" Margin="10" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="UserID" Binding="{Binding UserId}" Width="Auto"/>
                            <DataGridTextColumn Header="FullName" Binding="{Binding FullName}" Width="2*"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
                            <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="*"/>
                            <DataGridTextColumn Header="BirthDate" Binding="{Binding Birthdate, StringFormat={}{0:dd/MM/yyyy}}" Width="*"/>
                            <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StatusBar Grid.Row="3" Grid.ColumnSpan="2">
                        <StatusBarItem Content="Số lượng giáo viên: " />
                        <StatusBarItem x:Name="lecturerCount" Content="0"/>
                    </StatusBar>
                </Grid>
            </TabItem>


        </TabControl>
    </Grid>
</Window>
