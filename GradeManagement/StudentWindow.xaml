<Window x:Class="GradeManagement.StudentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradeManagement"
        mc:Ignorable="d"
        Title="StudentWindow" Height="450" Width="800" 
        Loaded="Window_Loaded" WindowState="Maximized"
        WindowStyle="SingleBorderWindow" ResizeMode="CanResize" FontFamily="Segoe UI">

    <Grid Background="#F4F6F8" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="1.2*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel -->
        <Grid Background="White" Margin="10" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="STUDENT LIST" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10" Grid.Row="0"/>

            <!-- Search Box -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="1">
                <TextBox x:Name="txtSearch" Width="200" Height="30"
                 ToolTip="Search by Name or StudentID"
                 FontSize="14" VerticalContentAlignment="Center"
                 Background="#F0F0F0" BorderBrush="#DDD" Padding="5" />
                <Button x:Name="btnSearch" Content="Search" Width="80" Height="30"
                Click="btnSearch_Click" Margin="10,0,0,0"
                Background="#3498DB" Foreground="White" BorderBrush="Transparent"
                Cursor="Hand"/>
                <Button x:Name="btnRefesh" Content="Refresh" Width="80" Height="30"
                Click="btnRefesh_Click" Margin="10,0,0,0"
                Background="#95A5A6" Foreground="White" BorderBrush="Transparent"
                Cursor="Hand"/>
            </StackPanel>

            <!-- DataGrid -->
            <Border CornerRadius="6" BorderBrush="#DDD" BorderThickness="1" Grid.Row="2">
                <DataGrid x:Name="dgStudents"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="dgStudents_SelectionChanged"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="#FAFAFA"
                  Background="White"
                  GridLinesVisibility="None"
                  RowBackground="White"
                  ColumnHeaderHeight="30"
                  BorderThickness="0"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  MaxHeight="600">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="StudentID" Binding="{Binding StudentId}" Width="Auto"/>
                        <DataGridTextColumn Header="FullName" Binding="{Binding FullName}" Width="Auto"/>
                        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="160"/>
                        <DataGridTextColumn Header="Dob" Binding="{Binding Dob}" Width="70"/>
                        <DataGridTextColumn Header="RollNumber" Binding="{Binding RollNumber}" Width="70"/>
                        <DataGridTextColumn Header="Course" Binding="{Binding ClassCodesWithSubjectId}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Right Panel -->
        <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Top" Background="White"  >
            <TextBlock Text="GRADE MANAGEMENT" FontSize="20" FontWeight="Bold" 
                       Foreground="#E74C3C" TextAlignment="Center" Margin="0,0,0,30"/>

            <Button x:Name="btNewStudent" Content="➕ ADD NEW STUDENT" Height="40" Margin="0,0,0,20"
                    Background="#2ECC71" Foreground="White" BorderBrush="Transparent" Cursor="Hand"
                    Click="Button_Click_Addnew"/>

            <Button x:Name="Bt_Grade" Content="📋 Grade For Student" Height="40" Margin="0,0,0,20"
                    Background="#2980B9" Foreground="White" BorderBrush="Transparent" Cursor="Hand"
                    Click="Bt_Grade_Click"/>

            <Button x:Name="Bt_Logout" Content="🚪 Logout" Height="40"
                    Background="#E67E22" Foreground="White" BorderBrush="Transparent" Cursor="Hand"
                    Click="Bt_StudentGrade_Click"/>
        </StackPanel>
    </Grid>
</Window>
